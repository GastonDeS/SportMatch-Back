{
  "name": "sports-match-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 restart all",
    "clean": "rimraf dist/*",
    "tsc": "tsc",
    "build": "npm-run-all clean tsc move",
    "prod": "npm-run-all build start",
    "move": "ts-node copyAssets",
    "first-start": "node ./dist/server.js",
    "deploy": "npm-run-all build first-start",
    "login": "aws codeartifact login --tool npm --repository backend-shared-libs --domain td --namespace td --profile npm --region us-east-1",
    "dev": "nodemon src/server.ts",
    "dev:start": "node dist/server.js",
    "lint": "eslint src/**/*.ts --fix"
  },
  "defaultExtension": "ts",
  "engines": {
    "node": ">=18.0.0"
  },
  "lint-staged": {
    "src/**/*.ts": "eslint src/**/*.ts --fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "~0.21.1",
    "bluebird": "~3.7.2",
    "body-parser": "~1.19.0",
    "cors": "~2.8.5",
    "dotenv": "~8.2.0",
    "express": "~4.17.1",
    "express-rate-limit": "~5.1.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "nodemon": "^3.0.1",
    "ts-node": "~8.8.1",
    "typescript": "^5.1.6"
  }
}
